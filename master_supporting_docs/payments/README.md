# Pilot bonuses

`pilot_bonuses.txt` records the bonus-payment list (Prolific ID, £ amount) issued during the pilot wave. Originally lived in `data/Datawork/p.txt`. Moved here as the canonical paper trail for participant payments.

Format: `<prolific_id>,<bonus_in_GBP>` per line, no header.
